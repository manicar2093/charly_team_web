---
interface Props {
    title: string
    subtitle: string
    paragraph: string
}

const {title, subtitle, paragraph} = Astro.props;
---

<div class="section-title">
    <h2>{title}</h2>
    <h3>{subtitle}</h3>
    <p>{paragraph}</p>
</div>